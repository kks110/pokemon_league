
<div class="card w-20">
  <ul class="list-group list-group-flush">
    <% @pairings.each do |p1, p2| %>
      <li class="list-group-item">
        <div class="row">
          <div class="col-5 text-start">
            <%= p1.name %>
          </div>
          <div class="col-2 text-center">
            vs
          </div>
          <div class="col-5 text-end">
            <%= p2 == 'Bye' ? p2 : p2.name %>
          </div>
        </div>
      </li>
    <% end %>
  </ul>
</div>
